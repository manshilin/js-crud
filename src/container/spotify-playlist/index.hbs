<main class="main">
    {{#with data}}
    <div class="main__content">
        <h1 class="title title--center">{{name}}</h1>
        <a href="/spotify-playlist-add?playlistId={{playlistId}}" class="add-button">
        <img src="/svg/add_button.svg" alt="">
        Додати до плейлиста
        </a>
    </div>
       <ul class="track__list">
        {{#each tracks}}
        <li class="track">
            <img src="{{image}}" alt="{{name}}">
       
        <div class="track__content">
        <strong class="track__name">{{name}}</strong>
        <em class="track__author">{{author}}</em>
        </div> 
        <a href="/spotify-delete-track?playlistId={{../playlistId}}&trackId={{id}}">
            <img src="/svg/button_delete.svg" alt="">
        </a>

        </li>
           
        {{/each}}

       </ul>
    {{/with}}
</main>